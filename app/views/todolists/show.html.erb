<% if flash[:notice] %>
  <div class="flash">
    <%= flash[:notice] %>
  </div>
<% end %>

<h4 class="show-title">Title:<%= @book.title %></h4>
  <h4 class="show-body">Body:<%= @book.body %></h4>
<div class="show-bottom">
  <%= link_to 'Edit', edit_todolist_path(@book.id) %>
  <%= link_to 'Back', '/todolists' %>
</div>



